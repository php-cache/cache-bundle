aequasi_cache:
    instances:
        memcached:
            type: memcached
            hosts:
                - { host: localhost, port: 11211 }
            persistent: true
        redis:
            type: redis
            hosts:
                - { host: localhost, port: 6379 }